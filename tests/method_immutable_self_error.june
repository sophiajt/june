// error: 'self' variable is not mutable

struct allocator Box {
    width: i64

    fun grow_width(self) {
        .width = 99
    }
}

fun main() {
    let box = new shared Box(width: 22)
    box.grow_width()

    println(box.width)
}