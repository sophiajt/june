// output: 88

struct Stats {
    age: i64
}

enum State {
    Stats(shared Stats)
}

fun helper() -> shared State {
    let y = State::Stats(new shared Stats(age: 88))

    return y
}

fun main() {
    let z = helper();

    match z {
        State::Stats(x) => println(x.age)
    }
}