// output: 200

struct Stats {
    age: i64
}

struct Employee {
    stats: shared Stats
}

fun main() {
    mut employee = new shared Employee(stats: new shared Stats(age: 100));
    let employee2 = new shared Employee(stats: new shared Stats(age: 200));

    employee.stats = employee2.stats;

    println(employee.stats.age)
}