struct Stats {
    age: i64
}

fun foo(a: Stats, b: Stats) -> Stats {
    if true {
        return a
    } else {
        return b
    }
}

fun main() {
    let f = foo(new shared Stats(age: 22), new shared Stats(age: 33))

    println(f.age)
}