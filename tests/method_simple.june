// output: 100

struct Box {
    width: i64

    fun get_area(self) -> i64 {
        return .width * .width
    }
}

fun main() {
    let box = new shared Box(width: 10)

    println(box.get_area())
}